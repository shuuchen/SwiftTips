// swift tips
// String & Character

// remove the last character
str.removeAtIndex(str.endIndex.predecessor())

// remove the last 5 chars
let endIdx = str.endIndex
let range = endIdx.advancedBy(-5)..<endIdx
str.removeRange(range)

// loop
for c in str.characters {
  ...
}

// index loop
for i in str.characters.indices {
  ...
}

// prefix and suffix
let str = "kokokokppp"
str.hasPrefix("koko") // true
str.hasSuffix("ppp") // true
